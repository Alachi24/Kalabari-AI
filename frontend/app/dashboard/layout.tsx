import React from "react"
export const metadata = {
  title: 'LinguaAI - Dashboard',
  description: 'Your translation history and saved translations',
}

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
