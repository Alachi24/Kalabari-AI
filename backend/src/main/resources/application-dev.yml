spring:
  jpa:
    show-sql: true
  flyway:
    clean-on-validation-error: true

model:
  server:
    url: http://localhost:8000
    # Use the in-memory mock client until the fine-tuned model server is deployed.
    mock: ${MODEL_SERVER_MOCK:true}

logging:
  level:
    com.example.nlp: DEBUG
    org.springframework.security: DEBUG
